module Data.BAByNF.Test.Util where

-- -- import qualified Test.Tasty as T
-- import qualified Test.Tasty.HUnit as TH

-- data FuncTest a b = FuncTest { ftInput :: a, ftExpected :: b, ftFunction :: a -> b }

-- ftRun :: (Show a, Show b, Eq b) => FuncTest a b -> TH.Assertion
-- ftRun FuncTest { ftInput = a, ftExpected = b, ftFunction = fn } =
--     TH.assertEqual name (fn a) b
--     where name = "Expect fn " ++ (show a) ++ " == " ++ (show b)

